# SERP-Intent-Analyzer
n8n workflow that takes a keyword, pulls live Google SERP data via SerpAPI, and uses Gemini AI to classify search intent, define the target audience, recommend a content angle, and generate an AEO tip for AI search visibility
